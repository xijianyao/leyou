server:
  port: 10087
spring:
  application:
    name: ly-registry
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10087/eureka
    register-with-eureka: false
    fetch-registry: false